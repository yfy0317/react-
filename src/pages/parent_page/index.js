import React from "react";
import style from "./theme_conf/mixins.scss";

export default class parentComponent extends React.Component {
  constructor(props) {
    super(props);
    // window.document.documentElement.setAttribute('data-theme', 'theme2')
  }
}
